package com.dodo.accounting.ui.viewmodel

/**
 * Shared UI enums used across tab ViewModels and screens.
 */

enum class ExportFormat {
    JSON,
    CSV
}

enum class HomePeriod {
    WEEK,
    MONTH,
    YEAR,
    CUSTOM
}

enum class StatsRangeMode {
    WEEK,
    MONTH,
    YEAR,
    CUSTOM
}
